server:
  port: 8763
  servlet:
    context-path: /filesystem
spring:
  application:
    name: file-system
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  profiles:
    active: dev
    include: admin